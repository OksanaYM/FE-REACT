import {simpsons} from "./sim.ts";


export const SimInfo = () => {
    return(
        <ul>
            {
                simpsons.map((value, index) => {
                    return
                })
            }
        </ul>
    )
}